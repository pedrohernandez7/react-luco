# Ejemplo de variables de entorno para Netlify
# Copia este archivo a .env para desarrollo local o configura en el panel de Netlify

# Variables de entorno de la aplicación
# NODE_ENV=production

# Variables específicas de Netlify (estas las configura Netlify automáticamente)
# NETLIFY=true
# NETLIFY_DEV=true (solo en entorno de desarrollo local con Netlify CLI)
# CONTEXT=production|deploy-preview|branch-deploy
